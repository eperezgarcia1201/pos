# Cloud control plane API
CLOUD_API_URL=http://localhost:8080

# Edge (on-site) backend API
EDGE_API_URL=http://localhost:8080

# Bootstrap (one-time) OR pre-registered node creds
STORE_ID=
BOOTSTRAP_TOKEN=
NODE_ID=
NODE_TOKEN=
NODE_LABEL=Store Edge Node
SOFTWARE_VERSION=0.1.0

# Poll interval in milliseconds
POLL_INTERVAL_MS=5000

# Edge authentication strategy (pick one)
# 1) Static bearer token + user id
EDGE_AUTH_BEARER=
EDGE_USER_ID=

# 2) PIN login
EDGE_PIN=

# 3) Username/password login
EDGE_USERNAME=
EDGE_PASSWORD=
